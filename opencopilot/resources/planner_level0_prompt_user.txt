User Inquiry:

{{ user_objective }}

Before crafting the plan, follow the following preparatory steps in sequence:

Task Identification: Consider the necessary tasks in straightforward English.
Task Validation: For each task, determine its relevance and whether you should create mulitple instances of it based on its description. Rewrite tasks if a change is required.
Task Assessment: Reflect on the tasks for potential revisions, like additions, removals or dependency. Rewrite tasks if a change is required.
Plan Formation: Compile the finalized task list into a valid JSON format.